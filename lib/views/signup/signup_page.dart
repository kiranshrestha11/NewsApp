import 'package:flutter/material.dart';
import 'package:news_app/views/signup/components/body.dart';

class SignpPage extends StatelessWidget {
  const SignpPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
